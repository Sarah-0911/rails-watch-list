<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%=asset_path("banner_cine.jpg")%>)";>
  <div class="container text-center">
    <h1>Save any kind of movies</h1>
  </div>
</div>

<h4>My lists</h4>
<ul class="list">
  <% @lists.each do |list| %>
  <li class="item"><%= link_to "#{list.name}", list_path(list) %></li>
  <% end %>
</ul>

<p class="btn btn-flat"><%= link_to "Add a new list", new_list_path %></p>
<%= link_to lists_path, data: {turbo_method: :delete, turbo_confirm: "Alert ! Tu vas tout faire péter 🦙🦙🦙!"} do %>
  <%= image_tag("red_button.jfif", width: '80',) %>
<% end %>

<%= render "partials/cards" %>
